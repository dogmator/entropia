/**
 * World geometry constants.
 * Defines spatial parameters for the virtual world domain.
 */

/** Linear dimension of the cubic virtual world domain in abstract space units. */
export const WORLD_SIZE = 600;

/** Spatial discretization parameter (hash cell size) for O(1) neighbor search. */
export const CELL_SIZE = 80;
