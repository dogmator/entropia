/**
 * Централізований реєстр конфігураційних параметрів системи рендерингу.
 *
 * Забезпечує дотримання принципу DRY (Don't Repeat Yourself) шляхом агрегації
 * всіх магічних чисел та порогових значень у єдиному модулі.
 */

/**
 * Параметри проекційного детектування (raycasting) для різних типів геометрії.
 */
export const RAYCASTER_CONFIG = {
  /** Поріг чутливості для точкових примітивів (Points). */
  pointsThreshold: 2.0,

  /** Поріг чутливості для лінійних примітивів (Line). */
  lineThreshold: 1.0,
} as const;

/**
 * Конфігурація поведінки інформаційного вікна (tooltip).
 */
export const TOOLTIP_CONFIG = {
  /** Тимчасова затримка перед деактивацією видимості (мс). */
  fadeOutDelay: 180,

  /** Горизонтальний зсув відносно позиції курсора (пікселі). */
  offsetX: 20,

  /** Вертикальний зсув відносно позиції курсора (пікселі). */
  offsetY: 20,
} as const;

/**
 * Параметри frustum culling та оптимізації рендерингу.
 */
export const CULLING_CONFIG = {
  /** Активація агресивного відсікання об'єктів поза межами видимості. */
  enableFrustumCulling: true,

  /** Мінімальна відстань до камери для рендерингу дрібних об'єктів. */
  minRenderDistance: 0.1,

  /** Максимальна відстань до камери (far plane). */
  maxRenderDistance: 5000,
} as const;

/**
 * Параметри адаптивної системи якості.
 */
export const ADAPTIVE_QUALITY_CONFIG = {
  /** Цільова частота кадрів для підтримки високої якості. */
  targetFPS: 55,

  /** Критичний поріг FPS для автоматичного зниження якості. */
  criticalFPS: 30,

  /** Кількість послідовних повільних кадрів для тригерування адаптації. */
  slowFrameThreshold: 10,

  /** Інтервал перевірки продуктивності (кадри). */
  checkInterval: 60,
} as const;

/**
 * Обмеження для запобігання нестабільності при втраті фокусу вікна.
 */
export const FRAME_TIMING_CONFIG = {
  /** Максимально допустима delta time (секунди). */
  maxDeltaTime: 0.1,

  /** Мінімально допустима delta time для запобігання ділення на 0. */
  minDeltaTime: 0.001,
} as const;
