/**
 * Entropia 3D — Переліки та детерміновані константи.
 *
 * Використовує патерн "const assertion" для забезпечення
 * літеральних типів та автодоповнення в IDE.
 *
 * @module shared/types/enums
 */

// ============================================================================
// ТИПИ СУТНОСТЕЙ
// ============================================================================

/**
 * Класифікація типів сутностей у симуляції.
 */
export const EntityType = {
  /** Травоїдний організм — первинний консумент. */
  PREY: 'PREY',
  /** Хижий організм — вторинний консумент. */
  PREDATOR: 'PREDATOR',
  /** Енергетичний ресурс. */
  FOOD: 'FOOD',
  /** Статична перешкода. */
  OBSTACLE: 'OBSTACLE',
} as const;

export type EntityType = (typeof EntityType)[keyof typeof EntityType];

// ============================================================================
// ПІДТИПИ ХИЖАКІВ
// ============================================================================

/**
 * Еволюційні підтипи хижих суб'єктів.
 */
export const PredatorSubtype = {
  /** Швидкий мисливець з низькою витривалістю. */
  HUNTER: 'HUNTER',
  /** Засідник з високою ударною потужністю. */
  AMBUSHER: 'AMBUSHER',
  /** Зграйний хижак з соціальною кооперацією. */
  PACK: 'PACK',
} as const;

export type PredatorSubtype = (typeof PredatorSubtype)[keyof typeof PredatorSubtype];

// ============================================================================
// СТАНИ ОРГАНІЗМІВ
// ============================================================================

/**
 * Фазові стани життєдіяльності організму.
 */
export const OrganismState = {
  /** Спокій — відсутність активної поведінки. */
  IDLE: 'IDLE',
  /** Пошук — активний рух до ресурсу. */
  SEEKING: 'SEEKING',
  /** Втеча — ухилення від загрози. */
  FLEEING: 'FLEEING',
  /** Полювання — переслідування жертви. */
  HUNTING: 'HUNTING',
  /** Репродукція — процес розмноження. */
  REPRODUCING: 'REPRODUCING',
  /** Агонія — термінальний стан. */
  DYING: 'DYING',
} as const;

export type OrganismState = (typeof OrganismState)[keyof typeof OrganismState];

// ============================================================================
// ЕКОЛОГІЧНІ ЗОНИ
// ============================================================================

/**
 * Типи екологічних зон у віртуальному світі.
 */
export const ZoneType = {
  /** Оазис — зона високої проліферації ресурсів. */
  OASIS: 'OASIS',
  /** Пустеля — зона енергетичного дефіциту. */
  DESERT: 'DESERT',
  /** Мисливські угіддя — територія з перевагою для хижаків. */
  HUNTING_GROUND: 'HUNTING_GROUND',
  /** Притулок — захищена зона для травоїдних. */
  SANCTUARY: 'SANCTUARY',
} as const;

export type ZoneType = (typeof ZoneType)[keyof typeof ZoneType];

// ============================================================================
// НАЛАШТУВАННЯ ГРАФІКИ
// ============================================================================

/**
 * Рівні інтенсивності графічної репрезентації.
 */
export const GraphicsQuality = {
  LOW: 'LOW',
  MEDIUM: 'MEDIUM',
  HIGH: 'HIGH',
  ULTRA: 'ULTRA',
  /** Користувацькі параметри. */
  CUSTOM: 'CUSTOM',
} as const;

export type GraphicsQuality = (typeof GraphicsQuality)[keyof typeof GraphicsQuality];
