/**
 * Entropia 3D — Параметри фізичної симуляції.
 *
 * Константи для алгоритмів Boids та рульової поведінки.
 *
 * @module shared/config/physics
 */

// ============================================================================
// ОСНОВНІ ФІЗИЧНІ ПАРАМЕТРИ
// ============================================================================

export const PHYSICS = {
  /** Дискретний крок інтегрування (1/60 секунди). */
  dt: 1 / 60,

  /** Коефіцієнт опору (в'язкості) середовища. */
  drag: 0.96,

  /** Вага сили сепарації (уникнення колізій). */
  separationWeight: 2.5,

  /** Вага сили вирівнювання (групова когерентність). */
  alignmentWeight: 1.2,

  /** Вага сили когезії (тяжіння до центру групи). */
  cohesionWeight: 1.0,

  /** Пріоритет вектора прагнення до цілі. */
  seekWeight: 3.5,

  /** Пріоритет вектора уникнення загроз. */
  avoidWeight: 3.5,

  /** Максимальна сила рулювання. */
  maxSteeringForce: 0.5,

  /** Радіус зони сепарації. */
  separationRadius: 18,

  /** Дистанція початку ухилення від перешкод. */
  obstacleAvoidanceDistance: 25,
} as const;

// ============================================================================
// ПАРАМЕТРИ ВЗАЄМОДІЇ
// ============================================================================

export const INTERACTION = {
  /** Дисипація енергії при відбитті від перешкод (0.8 = 20% втрат). */
  obstacleBounceDamping: 0.8,

  /** Множник сили виштовхування від перешкод. */
  obstaclePushMultiplier: 1.1,

  /** ККД засвоєння енергії хижаком при полюванні. */
  predatorEnergyEfficiency: 0.6,

  /** Мінімальний енергетичний притік при полюванні. */
  minEnergyGain: 25,
} as const;
